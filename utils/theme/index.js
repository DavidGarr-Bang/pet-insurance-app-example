import { DefaultTheme, Provider as PaperProvider } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#4373e2",
    accent: "#f1c40f",
  },
};
